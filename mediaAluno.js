const notas = [];
notas.push(10, 7.5, 5, 8)
let soma = 0
let elementos 

//=> Armazenar dentro de uma constante o total das notas do aluno
const total = notas.reduce((acc, curr) = acc + curr)

const total = notas.reduce((acc, curr) => acc + curr)
for(let index = 0; index < array.length; index ++) {
    const nota = notas[index] ; 
    soma = nota + soma
}

//=> Calcular a média das notas do aluno
//=> Retornar o resultado da média


